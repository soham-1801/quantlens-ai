# Database configuration (Defaults to local PostgreSQL, falls back to SQLite if not set)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/quantlens_db

# JWT Configuration
SECRET_KEY=yoursecretkeyherechangeinproduction1234567890
ACCESS_TOKEN_EXPIRE_MINUTES=1440
